# Native projects are generated by `expo prebuild` on EAS — never upload local copies.
/android
/ios

# Dependencies (EAS reinstalls)
node_modules/

# Expo / build output
.expo/
dist/
web-build/
expo-env.d.ts

# Build artifacts
*.apk
*.aab
*.ipa

# OS / editor cruft
.DS_Store
*.pem
*.orig.*
.kotlin/
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
